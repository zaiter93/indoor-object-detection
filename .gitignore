# Data: the raw dataset and everything derived from it. Too large for git;
# fetched by the Colab notebook / setup instructions instead.
Indoor Object Detection Dataset/
*.zip
data/raw/
data/interim/
data/processed/

# Training artefacts
runs/
*.pt
!weights/best.pt
predictions/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Submission bundle (regenerated by scripts/package_submission.py)
submission.zip

# Reference paper (kept locally, not part of the deliverable)
*.pdf

# Results from a training run. Committed only after the real Colab run --
# never from the local 2-epoch smoke test, whose numbers are meaningless.
reports/metrics.json
reports/results.md
reports/figures/qualitative_*.png
